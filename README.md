# Voting-Application
A simple Java-based voting system that allows users to register, log in, and cast a single vote securely. The application ensures one user–one vote functionality and displays real-time vote counts, demonstrating core OOP concepts and basic data handling in Java.
